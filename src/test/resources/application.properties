# Dummy value to satisfy Google GenAI embedding auto-configuration in tests.
# The embedding provider is not called during `contextLoads`.
spring.ai.google.genai.embedding.api-key=test

# The production migration uses SQL Server system catalogs and is applied
# against SQL Server when the application starts.
spring.flyway.enabled=false

